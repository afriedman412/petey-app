TODO
====

[ ] Wire up pages_per_chunk for table mode
    - Add "Split pages individually" toggle in builder UI (table mode only)
    - When enabled, use extract_pages_async instead of async_extract on the server
    - Default pages_per_chunk=1, with optional override for multi-page tables
    - Keeps full-document mode as default for query mode and table mode with metadata

[ ] Dynamic concurrency based on document size
    - Check file sizes upfront; if median is small (e.g. <50KB), bump concurrency from 5 to 15-20
    - File size is a good proxy for token count without needing to extract text first

[ ] Metadata propagation for pagewise table extraction
    - When splitting pages, backfill metadata fields (e.g. name, district) from page 1 into all rows
    - Could be schema-level config (propagate: true) or automatic detection
    - Alternative: users just set pages_per_chunk high enough to include metadata page

[ ] Remove requirements.txt (redundant with pyproject.toml)
